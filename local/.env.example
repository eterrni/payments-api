# Скопируйте в .env и при необходимости измените значения.
# Запуск: из корня проекта — go run ./cmd (после export $(cat local/.env | xargs) или source local/.env).

DB_DSN=host=localhost port=5432 user=postgres password=postgres dbname=payments sslmode=disable
